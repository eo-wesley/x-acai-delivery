{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "watchPatterns": [
            "apps/backend/**",
            "package.json"
        ]
    },
    "deploy": {
        "startCommand": "npm run start --prefix apps/backend",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 100,
        "restartPolicyType": "ON_FAILURE"
    }
}